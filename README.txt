Only replace server.js in the same location in your current deployed project. This is based on JP_REMINDER_ALL_IN_ONE_FINAL server.js. Owner notifications 'Also send email' will prefer existing RESEND_API_KEY and RESEND_FROM_EMAIL, otherwise SMTP fallback. No other function changed. Current HTML still says 'if SMTP configured'; existing checkbox works with Resend after deploy. Keep current latest public/index.html, public/public/index.html, jp-practice-consent.js and owner-practice-reminders.html unchanged. First test with ONE selected candidate, not Send to All. Resend failures can lead to partial delivery; do not immediately resend to all.
