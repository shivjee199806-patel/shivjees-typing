Corrections batch 1-7
- Topic keyword chips shown in passage picker; generated titles are topic-indexed and typed matter contains no exam/difficulty filler prefix.
- Exact duplicate guard prevents identical matter under different passage IDs in the same exam.
- Highlight defaults are exam-driven, not globally forced; candidate highlight choice remains available unless Owner locks it.
- Backspace uses the selected exam default; Owner can edit the exam default.
- Ready Live bank remains 40 passages and Edit Matter opens a full visible editor (title + complete matter).
- Live attempt inherits the selected actual exam settings from the server.
- Passage display groups each word so a word cannot split across visual lines.
- Cancel from typing test returns one step to Instructions; Back then returns to passage list.
- Existing payment/block/login/About behavior preserved.
